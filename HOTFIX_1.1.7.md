# Hotfix 1.1.7

- Flex Consumption Deploy aktiviert (`sku: flexconsumption`)
- Remote Build für Azure Functions aktiviert
- Legacy-URL `https://<APP_NAME>.azurewebsites.net` vollständig entfernt
- neue Pflichtvariable `INVESTMENT_API_BASE_URL` für die echte Flex-Standarddomäne
- Backend-Health-Check und Android-APK verwenden dieselbe URL
