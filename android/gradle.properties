org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# AGP 9.x built-in Kotlin bleibt aktiviert (Standard).
# Kein org.jetbrains.kotlin.android Plugin mehr notwendig.

# Lokal ersetzen oder beim GitHub-Build als -P Wert uebergeben.
INVESTMENT_API_BASE_URL=https://YOUR-FUNCTION-APP.azurewebsites.net
FIREBASE_APP_ID=
FIREBASE_API_KEY=
FIREBASE_PROJECT_ID=
FIREBASE_SENDER_ID=
