name: Deploy Backend

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/backend-deploy.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        working-directory: backend
        run: npm install --no-audit --no-fund

      - name: Syntax check
        working-directory: backend
        run: npm run check

      - name: Check Azure configuration
        id: azure_config
        shell: bash
        env:
          APP_NAME: ${{ vars.AZURE_FUNCTIONAPP_NAME }}
          PUBLISH_PROFILE: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
        run: |
          if [ -n "$APP_NAME" ] && [ -n "$PUBLISH_PROFILE" ]; then
            echo "configured=true" >> "$GITHUB_OUTPUT"
            echo "Azure deployment configuration found."
          else
            echo "configured=false" >> "$GITHUB_OUTPUT"
            echo "## Backend Deploy" >> "$GITHUB_STEP_SUMMARY"
            echo "❌ Azure ist noch nicht eingerichtet. Es wurde kein Backend veröffentlicht." >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Benötigt:" >> "$GITHUB_STEP_SUMMARY"
            echo "- Variable: AZURE_FUNCTIONAPP_NAME" >> "$GITHUB_STEP_SUMMARY"
            echo "- Secret: AZURE_FUNCTIONAPP_PUBLISH_PROFILE" >> "$GITHUB_STEP_SUMMARY"
            echo "Azure configuration missing; stopping before a misleading green deploy."
            exit 2
          fi

      - name: Deploy Azure Functions
        if: steps.azure_config.outputs.configured == 'true'
        uses: Azure/functions-action@v1
        with:
          app-name: ${{ vars.AZURE_FUNCTIONAPP_NAME }}
          package: backend
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}

      - name: Verify live backend
        if: steps.azure_config.outputs.configured == 'true'
        shell: bash
        env:
          APP_NAME: ${{ vars.AZURE_FUNCTIONAPP_NAME }}
        run: |
          URL="https://${APP_NAME}.azurewebsites.net/api/health"
          echo "Checking $URL"
          for attempt in 1 2 3 4 5 6; do
            if curl --fail --silent --show-error --max-time 20 "$URL"; then
              echo ""
              echo "## Backend Deploy" >> "$GITHUB_STEP_SUMMARY"
              echo "✅ Backend ist live: https://${APP_NAME}.azurewebsites.net" >> "$GITHUB_STEP_SUMMARY"
              exit 0
            fi
            sleep 10
          done
          echo "❌ Deploy wurde ausgeführt, aber /api/health ist nicht erreichbar." >> "$GITHUB_STEP_SUMMARY"
          exit 1

